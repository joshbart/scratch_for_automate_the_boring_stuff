#!/usr/bin/env python

# is_raining = api.get_weather()
# have_umbrella = parameter.read()

if is_raining == True:
    if have_umbrella == False:
        while is_raining == True:
            # sleep 5 minutes
            # is_raining = api.get_weather()

print("Go outside!")